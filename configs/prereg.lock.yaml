# ═══════════════════════════════════════════════════════════════════════════
# FROZEN at git tag `prereg-v1`. Do not edit.
#
# Stage 10 (metrics_and_anchors) hashes the live configs against this file and
# refuses to run on a mismatch. To change anything here, log the change in
# docs/deviations.md, bump to prereg-v2, and say so in the write-up.
# ═══════════════════════════════════════════════════════════════════════════

study:
  name: "EmoCap: what does emotion-conditioning accuracy measure when the captions are synthetic?"
  emotions: [joyful, sad, tense, romantic, humorous]   # order fixes emotion_id -- DO NOT REORDER
  # Applied 2026-08-21 with the tag. The superseded values are kept below the live ones as
  # the record of what was drafted; `superseded_2026_08_21` is documentation, never read by
  # code. Reasons: the evaluation regime moved to 5-fold CV over each arm (MDE 2.3 against
  # 6.5 on a single 1k test split, which made a registered 5-point criterion unfalsifiable);
  # conditioning variants are dropped entirely, the decoder is now a fixed control; and
  # A_lstm is dropped because 8,076 captions cannot train a decoder from scratch.
  prompt_version: v10                   # must equal emocap.data.prompt.PROMPT_VERSION
  design: six_matched_data_arms
  # Three structure classes so provenance is never compared across shape:
  #   1/image   S_unpaired | V1_unpaired | H_unpaired
  #   5/image   S_paired5  | V1_paired5
  #   25/image  S_paired25 (only our corpus can supply it)
  arms: [S_paired25, S_paired5, S_unpaired, V1_paired5, V1_unpaired, H_unpaired]
  # 1-caption-per-image arms are matched at 4,390 (878 x 5), set by the scarcest register
  # under the strict trait mapping. Matching on COUNT as well as structure is required or
  # P1 confounds "human data" with "half as much data".
  unpaired_arm_cells: 4390
  paired5_arm_images: 8048
  paired25_arm_cells: 201900
  folds: 5
  fold_split_by: image_id                 # NEVER by caption: one image's 5 cells are dependent
  negative_control: shuffled_emotion_labels_per_arm
  runs: 36                                # 6 arms x 5 folds + 6 negative controls (1 fold each)
  ceiling_1225_cells: 0.620               # runs/gate-check-v5/result.json (was mis-cited as 0.612)
  # SIX comparisons are confirmatory and Holm-corrected; the other nine pairwise
  # combinations are exploratory, reported uncorrected and carrying no hypothesis.
  # Correcting over all 15 raises the MDE from 5.2 to 5.6 points at seed SD 0.02 and buys
  # nothing, because nine of them test no prediction.
  # CONFIRMATORY comparisons are MATCHED ON IMAGES. The human arm is Personality-Captions
  # over YFCC photographs; every other arm is Flickr8k. The image sets do not intersect, so
  # any human-vs-synthetic comparison varies provenance, image distribution and groundedness
  # at once (CLIPScore 0.578 human vs 0.792 ours, 2.64 SD apart). Those comparisons are
  # REFERENCE, not confirmatory, and are reported with the confound stated.
  confirmatory_comparisons:
    - [S_unpaired, V1_unpaired]       # P6 -- same 8,048 Flickr8k images, same structure
    - [S_paired5, V1_paired5]         # P6 -- same images, 5/image
    - [S_paired25, S_unpaired]        # P3a, P3b -- same corpus, structure varies
    - [S_paired25, S_paired5]         # P3 -- source-caption count
  reference_comparisons:              # reported, hypothesised, NOT claimed to isolate provenance
    - [H_unpaired, S_unpaired]        # P1, P2 -- cross-dataset
    - [H_unpaired, V1_unpaired]       # cross-dataset
  exploratory_comparisons: all_remaining_pairs
  # MDE is a FUNCTION of seed noise, which cannot be known before models exist. Registered
  # as a curve for the limiting comparison (1-caption-per-image arms, ~8,048 cells):
  #   seed SD  0.000 0.005 0.010 0.020 0.030
  #   MDE      2.7   2.9   3.5   5.2   7.1   (points, 6 comparisons, 80% power)
  # Criteria are set at >=6 points so they clear the MDE at seed SD 0.02.
  # governing curve: 4 confirmatory comparisons, n=4,390, 3 runs averaged.
  # Reproduce with `uv run python scripts/compute_mde.py`.
  mde_curve_by_seed_sd: {0.0: 0.032, 0.005: 0.035, 0.01: 0.042, 0.02: 0.063, 0.03: 0.088}
  mde_curve_5runs_optimistic: {0.0: 0.032, 0.005: 0.034, 0.01: 0.038, 0.02: 0.053, 0.03: 0.071}
  criterion_points: 0.07
  underpowered_rule: report_as_underpowered_if_observed_spread_implies_MDE_above_criterion
  # The v1 pilot corpus is an arm because it is the high-stereotypy end of the study's axis
  # (anchor 0.718 against 0.507 ours and 0.450 human, strict mapping, all @4,486 cells) AND because the pilot's
  # decoder faults are fixed here, so its data can be tested apart from its code.
  v1_arm_rationale: high_stereotypy_contrast_and_code_vs_data_separation

  # Personality-Captions traits -> our five registers. STRICT 1:1, frozen before training.
  #
  # Chosen over the grouped mapping deliberately. lab-notebook.md records that the sign of
  # the human-vs-synthetic stereotypy difference FLIPS between them (strict: human 0.450 vs
  # ours 0.440, ours less stereotyped; grouped: human 0.352 vs ours 0.441, ours more), and
  # concludes "the strict 1:1 mapping is the more defensible of the two and is what the
  # paper will privilege". It is also the mapping every human measurement in this repo was
  # actually computed under (scripts/compare_human_ceiling.py) -- the grouped variant had
  # never been run through any instrument.
  #
  # Cost of the choice: strict yields 4,486 usable cells, so ALL 1-caption-per-image arms
  # shrink to 4,390 (878 per register, the scarcest). That raises the MDE by 0.4 points at
  # seed SD 0.02 -- a price paid to avoid registering an unmeasured instrument whose choice
  # would have been forced by the arm size we wanted.
  personality_map_strict:
    joyful:   [Happy]
    sad:      [Gloomy]
    tense:    [Anxious]
    romantic: [Romantic]
    humorous: [Humorous]
  personality_map_mode: strict_1to1
  personality_map_grouped_REJECTED:
    reason: never_measured_and_flips_the_sign_of_the_stereotypy_comparison
    joyful:   [Optimistic, Cheerful, Happy, Enthusiastic, Energetic]
    sad:      [Gloomy, Melancholic, Miserable, Sentimental, Solemn]
    tense:    [Anxious, Paranoid, Fearful, Aggressive, Intense]
    romantic: [Romantic, Passionate, Sweet]
    humorous: [Sarcastic, Zany, Witty, Silly, Humorous]
  # seeds stay LIVE: they still fix initialisation and data order per run. What changed is
  # what is REPORTED as variance -- fold-to-fold spread, not seed spread. Keeping them here
  # is what makes a run reproducible; PREREGISTERED_SEEDS in code must match.
  seeds: [42, 1337, 2024]
  superseded_2026_08_21:
    conditions: [V0, V1, V2, V3, C]
    tracks: [A_lstm, B_clipcap]
    variance_reported_as: spread_across_three_seeds   # now: fold-to-fold spread

data:
  dataset: flickr8k
  neutral_source: human_annotations       # NOT a VLM. See docs/deviations.md if changed.
  captions_per_image: 5                   # Flickr8k ships 5 human captions
  references_per_cell: 5                   # one per human caption, per emotion
  split_source: official_flickr8k          # Hodosh et al. 2013 train/dev/test lists
  split_sizes: {train: 6000, val: 1000, test: 1000}
  split_by: image_id                       # never by row -- prevents leakage
  split_seed: 42                           # only for the 91 images outside the official lists
  # PROVISIONAL until the stage-02 probe finalises it; pinned before the prereg-v1
  # tag. gemini-2.0-flash (the pilot's model) has been retired and is no longer
  # served -- the config named a model that no longer exists. Must always be an
  # explicit version, never a moving alias, or the dataset's provenance is unknown.
  # The ONE generator key. An earlier edit added `generation_model` in data.yaml
  # instead of updating this, leaving the frozen config naming the retired v5
  # generator while the documents named 3.7-flash.
  generator_model: "gemini-3.7-flash"   # must equal data.yaml generation.model
  generation_input: image_plus_human_captions   # multimodal; Moondream retired
  generator_multimodal: true
  # Each register also returns a self-reported fit: 0 natural, 1 strained, 2 no honest
  # reading of this register exists for this caption.
  #
  # DECIDED 2026-08-20: RECORDED, NEVER FILTERED ON. Two reasons, the second decisive.
  #
  # 1. The flag does not do what filtering on it would assume. It was proposed as a way
  #    to find invented cells and fails: defect rate by strain level is 1.6% / 2.6% /
  #    2.4% -- flat. A cell marked "no honest reading exists" is no likelier to contain a
  #    grounding defect than one marked "natural".
  #
  # 2. Excluding strain-2 would wreck class balance. The corpus is perfectly even at
  #    40,380 cells per register; strain-2 is not:
  #        joyful 132 (0.3%) | tense 1,073 (2.7%) | sad 2,006 (5.0%)
  #        humorous 3,223 (8.0%) | romantic 6,942 (17.2%)
  #    Dropping them leaves 33,438-40,248 per register, a 16.9% imbalance concentrated
  #    exactly where the study is most delicate -- romantic would lose 52x as much data as
  #    joyful. Any later finding that romantic is harder would then be inseparable from
  #    romantic having less training data. A self-inflicted confound.
  #
  # It IS retained as a register-difficulty measure, which is analysis rather than
  # filtering. Absent on pre-2026-08-19 records, where a missing value means unknown,
  # never 0.
  generator_reports_strain: true
  strain_used_as_exclusion: false
  target_caption_words: {min: 8, max: 24}

decode:                                    # one DecodeConfig for every arm, so no arm can be advantaged by decode tuning
  beam_size: 4
  max_new_tokens: 40
  min_new_tokens: 4
  length_penalty: 1.0
  no_repeat_ngram_size: 3

metrics:
  primary: emotion_accuracy                # held-out classifier, top-1
  secondary: [cider_d, bleu_4, clipscore, distinctiveness]
  confirmatory: human_eval
  smallest_effect_of_interest: 0.07   # smallest effect worth caring about, arm vs arm
  bootstrap:
    resamples: 10000
    cluster_by: image_id                   # the 5 emotion cells of one image are not independent
    ci: 0.95
  multiple_comparisons: holm_bonferroni     # Holm over the 4 confirmatory comparisons; reference and exploratory are uncorrected

anchors:
  # UNCALIBRATED as registered -- see gates.ceiling_min below and §6 of the
  # preregistration. Human style-conditioned captions measure 0.726, not 0.85.
  ceiling: reference_captions
  floor: shuffled_emotion_labels            # expect ~0.20 (chance)
  # Third anchor, added 2026-08-18 after auditing the Part 0 sample, then substantially
  # revised on 2026-08-19. The rates and the value quoted here originally -- "bright" in
  # 31% of joyful, "alone" in 21% of sad, "soft" in 26% of romantic, and a keyword rule at
  # 53.9% -- were measured on the *v1 prompt*, which has since been replaced. Under the
  # final v5 prompt those rates are 14%, 1% and 6%. Do not quote the v1 figures as if they
  # described this corpus.
  #
  # So a large share of the PRIMARY metric is available without any emotional
  # register at all. Reporting the classifier alone would credit the captioning model
  # for keyword emission. This anchor makes that share explicit: the model's real
  # contribution is the margin above it, not the raw accuracy.
  # THE ANCHOR IS SAMPLE-SIZE DEPENDENT AND MUST NOT BE STORED AS A SINGLE NUMBER.
  # Measured on the generated corpus with the estimator below:
  #
  #     4,486 cells   0.440 (sd 0.007 over 10 image-disjoint subsamples)
  #    24,925 cells   0.403
  #   201,900 cells   0.332 (per-seed .3320 / .3324 / .3327)
  #
  # A keyword rule fitted on few images transfers well to held-out images from that same
  # narrow pool and degrades as the pool widens, so the anchor FALLS ~11 points across
  # this range. The decline is a property of the estimator, not of one corpus: the human
  # comparison corpus falls -0.034 over the same span where ours falls -0.031.
  #
  # RULE, so the two cannot be mismatched again: the anchor is RECOMPUTED on whatever
  # evaluation set the primary metric is measured on, at that set's own n, and reported
  # beside it. It is never read from a stored constant. Three claims were retracted on
  # 2026-08-19 for comparing an anchor at one n against accuracy at another -- see
  # docs/lab-notebook.md and docs/deviations.md.
  lexical_shortcut: top25_keyword_rule
  lexical_shortcut_estimator: cv5_by_image_id_3seeds_fractional_ties
  lexical_shortcut_rule: recompute_on_each_evaluation_set_at_its_own_n
  # Reference values, for context only -- NOT thresholds. Prompt v1 vs v5 at matched n
  # (1,225-cell audit sets) shows what the prompt rewrite bought: 0.641 -> 0.394.
  lexical_shortcut_reference:
    full_corpus_201900_cells: 0.332
    test_split_24925_cells: 0.403
    matched_4486_cells: 0.440
    v1_prompt_1225_cells: 0.641
    human_corpus_4486_cells: 0.450
  # Pre-registered expectation, so it is not discovered after the fact: `tense` and
  # `humorous` have the weakest lexical markers (grips/edge 12%; seemingly/quite 6%)
  # and should dominate the classifier's confusion matrix.
  #
  # LEFT AS REGISTERED, AND CONTRADICTED. Measured difficulty on the v5 Part 0 sample
  # (the model's own strain report over 1,225 cells) orders them romantic 1.05 >
  # humorous 0.90 > sad 0.89 > tense 0.85 > joyful 0.15. `tense` is NOT among the
  # hardest; `romantic` is, which the independent human judge found as well. The
  # prediction is not retrofitted -- the disagreement is the result.
  expected_hardest_registers: [tense, humorous]

gates:                                      # a run or the study halts if these fail
  manipulation_check:
    negative_control_accuracy_max: 0.25   # shuffled-label control, per arm (was: V0)
  # 0.85 was set a priori with NOTHING CALIBRATING IT, and it is very likely wrong.
  # Measured with the identical instrument at matched sample size, human-written
  # style-conditioned captions (Personality-Captions, 4,486 cells) reach only 0.726 -- so
  # this gate as written would halt a study on data matching human performance. That is a
  # miscalibrated instrument, not a finding about the corpus.
  #
  # Recalibrating against the measured human ceiling is legitimate PRE-TAG with the
  # reasoning logged, and indefensible afterwards. The value MUST be settled before
  # `prereg-v1` is applied. Until then 0.85 stands as registered.
  #
  # Two limits on the human figure: it is measured at 4,486 cells, the largest the human
  # corpus supports under a 1:1 trait mapping, and it is 5-way -- NOT comparable to binary
  # style-accuracy numbers reported on FlickrStyle10K or SentiCap.
  # Applied 2026-08-21. The registered `ceiling_min: 0.85` was uncalibrated: our corpus
  # reaches ~0.77 and human-written text reaches 0.726 on the same instrument, so the gate as
  # written would halt a study on data matching human performance. The ceiling is also
  # sample-size dependent (0.612 at 1,225 cells -> 0.773 at 201,900), so there is no single
  # number to threshold. Replaced by a detectability criterion.
  gate: floor_to_ceiling_range_must_admit_smallest_effect_at_design_MDE
  smallest_effect_of_interest: 0.07   # matches the §3 criteria; was 0.10 under the dropped design
  mde_governing_at_seed_sd_0.02: 0.063   # 4 comparisons, n=4,390, 3 runs averaged
  human_legibility_gate: human_blind_guess_must_exceed_same_corpus_anchor
  superseded_2026_08_21:
    ceiling_min: 0.85
    ceiling_human_reference: 0.726
  visual_dependence_probe: required         # captions must change when features are zeroed
  truncation_rate_max: 0.02

exclusions:                                 # the ONLY grounds for dropping a run
  - crashed_or_incomplete_training
  - nan_loss
  - failed_visual_dependence_probe
# Poor metric performance is explicitly NOT grounds for exclusion.

human_eval:
  items: 150
  raters: 3
  scales: [tone_match_1_5, grounding_1_5]
  reliability: krippendorff_alpha
  blinded: true
  attention_checks: true
